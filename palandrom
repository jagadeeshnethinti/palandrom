#include<stdio.h>
main()
{
	char a[10];
	int len,i,flag=0;
	scanf("%s",&a);
	len=strlen(a);
	for(i=0;i<len;i++)
	{
		if(a[i]!=a[len-i-1])
		{
			flag=1;
			break;
		}
	
	}	if(flag)
		{
			printf(" not a palindrom");
		}else
		{
			printf("palindrom");
		}
}
